<Page
    x:Class="GestioProjectes.Report"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GestioProjectes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" x:Name="btnGoGestio" Click="btnGoGestio_Click">Accede a la gestión de proyectos</Button>

        <WebView x:Name="wbvJasperReports" Grid.Row="1" Margin="10"
                     HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                     NavigationStarting="wbvJasperReports_NavigationStarting" NavigationCompleted="wbvJasperReports_NavigationCompleted"
                     DefaultBackgroundColor="White"></WebView>
    </Grid>
</Page>
